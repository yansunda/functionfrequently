<?php

rawurlencode(string $str);
//参数：要编码的url
//功能：将所有除了-_.之外的所有非字母数字的字符串都替换成百分号（%）后跟两个十六进制数。
urlencode(string $str)
//参数：要编码的url
//功能：次函数便于将字符串编码并将其用于URL的请求部分，同时它还便于将变量传递给下一页
//返回值：返回除了-_.之外的所有非字母数字字符都被替换成百分号（%）后跟两位十六进制数，空格则编码为加号（+）。此编码与表单POST数据的编码方式是一样的。由于历史原因，此编码在将空格编码为加号（+）方面与rawurlencode不同. 
rawurldecode(string $str)
//参数：要解码的字符串
//功能：返回解码后的URL字符串
//返回值：返回字符串中百分号（%）后跟两位十六进制的序列都将被替换成原义字符
urldecode(string $str)
//参数：解码已经编码的字符串
//功能：解码给出的已编码字符串中的任何%##。加号（+）被解码成一个空格字符。
//返回值：返回解码后的字符串


//应用一：对路径进行编码进行编码
$path = 
